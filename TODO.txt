Expose some store props via slot (e.g. currentQuestion)
Explore slots for questions with various types of content (e.g. text box) or <component is="">?
Move styles in a single css file and import it so that it is scoped
Reasses the aspect-ratio option